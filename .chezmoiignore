README.md
AGENTS.md
# to avoid reporting this files have changed
.oh-my-zsh/cache

# ecc is installed as a local Claude Code plugin by
# .chezmoiscripts/run_onchange_after_70-ecc-install.sh.tmpl; everything below is owned by
# that script / the plugin runtime, not chezmoi.
.claude/rules/ecc
.claude/commands
.claude/agents
.claude/plugins
# local design docs (not deployed to $HOME)
docs

{{ if eq .chezmoi.os "linux" }}
# macOS-only — LaunchAgents + the per-user ~/Library (Application Support, etc.)
Library
{{ end }}
{{ if eq .chezmoi.os "darwin" }}
# Linux-only — systemd user units (macOS uses launchd via private_Library instead)
.config/systemd
{{ end }}
