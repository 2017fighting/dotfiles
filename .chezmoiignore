README.md
AGENTS.md
CLAUDE.md
BOOTSTRAP.md
# local architecture glossary (not deployed to $HOME)
CONTEXT.md
# to avoid reporting this files have changed
.oh-my-zsh/cache

# ~/.claude/{commands,agents,plugins} are owned by the Claude Code plugin runtime
# and skill managers, not by chezmoi — never manage them here.
.claude/commands
.claude/agents
.claude/plugins
# local design docs (not deployed to $HOME)
docs

{{ if eq .chezmoi.os "linux" }}
# macOS-only — LaunchAgents + the per-user ~/Library (Application Support, etc.)
Library
{{ end }}
{{ if eq .chezmoi.os "darwin" }}
# Linux-only — systemd user units (macOS uses launchd via private_Library instead)
.config/systemd
{{ end }}
